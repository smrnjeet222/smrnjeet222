name: README build
on:
  push:
    branches:
      - master
  schedule:
    - cron: '30 14 * * 1-5'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Get Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Configure pip cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/cache/requirements.txt') }}
          restore-keys: ${{ runner.os }}-pip-
      - name: Install dependencies
        run: python -m pip install -r cache/requirements.txt
      - name: Update stats SVGs
        env:
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
          USER_NAME: smrnjeet222
        run: python today.py
      - name: Commit
        run: |-
          git add .
          git config --global user.name "Simranjeet Singh"
          git config --global user.email "48654626+smrnjeet222@users.noreply.github.com"
          git commit -m "Updated profile stats" -a || echo "No changes to commit"
          git push
